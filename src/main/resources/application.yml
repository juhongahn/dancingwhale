# Datasource 소스
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: wednesday5028@gmail.com
    password: zpoapnmapvxasumu
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 100MB
  thymeleaf:
    cache: false
  messages:
    basename: messages,errors
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: sa
    password:
  # H2 설정
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

# 세션 유지 시간 30분
server:
  error:
    whitelabel:
      enabled: false
  servlet:
    session:
      timeout: 1800
      tracking-modes: cookie
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace

# 상품 이미지 업로드 경로

upload:
  postImgLocation: C:/doinggajigaji/post
  uploadPath: file:///C:/doinggajigaji/
# 리소스 업로드 경로




